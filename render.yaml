services:
  - type: web
    name: ats-resume-ranker
    env: python
    region: singapore
    pythonVersion: "3.11.9"  # ← FIXED!
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/streamlit_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
