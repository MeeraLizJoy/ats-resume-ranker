services:
  - type: web
    name: ats-ranker-pro
    runtime: docker
    plan: free # Changed from starter to free
    envVars:
      - key: MY_API_KEY
        sync: false
      - key: RENDER
        value: "true"
      # We remove the disk section because Free doesn't support it